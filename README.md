# hello-world-script-243898-243912

Minimal Flask app.

## Run

```bash
cd hello_world_python_app
python main.py
```

By default it will bind to `HOST`/`PORT` (if set), otherwise `0.0.0.0:3001`.

## Endpoints

- `GET /` -> `Hello, World!`
- `GET /bye` -> `Bye`
